import React from 'react';
import PropTypes from 'prop-types';

const Home = props => {
    return (
        <div>
            <p>Home</p>
        </div>
    );
};

Home.propTypes = {

};

export default Home;
